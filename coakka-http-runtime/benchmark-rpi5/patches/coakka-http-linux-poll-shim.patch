diff --git a/src/runtime/coakka_http_linux_poll_shim.c b/src/runtime/coakka_http_linux_poll_shim.c
index aa95c38..4973bdc 100644
--- a/src/runtime/coakka_http_linux_poll_shim.c
+++ b/src/runtime/coakka_http_linux_poll_shim.c
@@ -18,6 +18,12 @@ int epoll_ctl(int descriptor, int operation, int target,
   return (int)syscall(SYS_epoll_ctl, descriptor, operation, target, event);
 }
 
+int epoll_wait(int descriptor, struct epoll_event *events, int event_count,
+               int timeout_ms) {
+  return (int)syscall(SYS_epoll_pwait, descriptor, events, event_count,
+                      timeout_ms, NULL, sizeof(uint64_t));
+}
+
 int epoll_pwait(int descriptor, struct epoll_event *events, int event_count,
                 int timeout_ms, const sigset_t *signal_mask) {
   return (int)syscall(SYS_epoll_pwait, descriptor, events, event_count,
