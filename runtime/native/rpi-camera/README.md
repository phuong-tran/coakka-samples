# Raspberry Pi Camera Sample Moved

The source and run guide now live at
[`runtime-streaming-demo/rpi-camera/`](../../../runtime-streaming-demo/rpi-camera/README.md).

This compatibility page keeps links embedded in the existing `1.1.0` archives
working. New integrations should use the top-level streaming-demo lane.
