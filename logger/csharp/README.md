# C# Logger Samples

C# logger samples consume the published `CoAkka.Logger` NuGet package from the
public artifact surface.

Run both C# logger samples:

```sh
bash run.sh logger csharp basic
bash run.sh logger csharp pressure
```
