# C# Logger Basic

Installs the published `CoAkka.Logger` package into a temporary console project,
emits one record, drains it, and prints logger counters.

```sh
bash run.sh logger csharp basic
```
