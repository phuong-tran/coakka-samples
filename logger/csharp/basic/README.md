# C# Logger Basic

Installs published `CoAkka.Logger==1.2.3` into a temporary `net8.0` console
project, emits one record, drains it, and prints logger counters.

```sh
bash run.sh logger csharp basic
```
